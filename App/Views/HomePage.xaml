<Page
    x:Class="App.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:contracts="using:App.Plugin.Contracts"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Grid Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Padding="20" Spacing="8">
                <TextBlock Text="Plugins" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <Button Content="Open Folder" HorizontalAlignment="Stretch" Click="OnPickFolderClick"/>
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" FontSize="12" Foreground="Gray" TextWrapping="Wrap"/>
            </StackPanel>

            <ListView Grid.Row="1" 
                      ItemsSource="{x:Bind ViewModel.DiscoveredPlugins, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedPlugin, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="contracts:PluginDiscoveryResult">
                        <StackPanel Margin="0,4">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind Version}" FontSize="10" Opacity="0.6"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Grid.Row="2" Content="Load Selected" Margin="20" 
                    HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.LoadSelectedPluginCommand}"
                    IsEnabled="{x:Bind ViewModel.IsLoadButtonEnabled, Mode=OneWay}"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <ContentControl Content="{x:Bind ViewModel.PluginContent, Mode=OneWay}"
                            HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>

            <!-- Placeholder (Only visible if nothing is loaded) -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12"
                        Visibility="{x:Bind ViewModel.PlaceholderVisibility, Mode=OneWay}">
                <SymbolIcon Symbol="Library" Foreground="{ThemeResource SystemControlForegroundBaseMediumLowBrush}">
                    <SymbolIcon.RenderTransform>
                        <ScaleTransform ScaleX="2" ScaleY="2"/>
                    </SymbolIcon.RenderTransform>
                </SymbolIcon>
                <TextBlock Text="Select a plugin to display content" Foreground="Gray"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>